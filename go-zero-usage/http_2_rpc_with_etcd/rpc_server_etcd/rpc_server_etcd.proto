syntax = "proto3";

package rpc_server_etcd;
option go_package="./rpc_server_etcd";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Rpc_server_etcd {
  rpc Ping(Request) returns(Response);
}
