syntax = "v1"

type Request {
	Name string `path:"name,options=you|me"`
}

type Response {
	Message string `json:"message"`
}

service hump_style-api {
	@handler Hump_styleHandler
	get /from/:name (Request) returns (Response)
}

